---
name: mtl
channels: [conda-forge, nodefaults]
dependencies:
  - python ==3.12
  - pip >=21.1

  # Development dependencies
  - conda-lock
  - ipykernel
  - jupyterlab

  # Packages
  - statsmodels
  - plotly >=5.2.0,<6
  - matplotlib
  - numpy
  - pandas >= 2.2
  - scikit-learn
  - plotly
  - arch-py
  - openpyxl

  # Pip dependencies
  - pip: [-e ., pdbp, kaleido, arch]
